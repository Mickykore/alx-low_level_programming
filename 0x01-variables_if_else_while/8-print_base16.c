#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char c;
int n;
for (n = 48; n <= 57; ++n)
{
putchar(n);
}
for (c = 'a'; n <= 'f'; ++n)
{
putchar(c);
}
putchar('\n');
return (0);
}
