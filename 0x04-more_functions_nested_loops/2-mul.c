#include "main.h"
/**
 * mul - a function to multiple two intiger
 * @a: first number to multiply
 * @b: second number to multiply
 *
 * Return: nothing
 */
int mul(int a, int b)
{
	return (a * b);
}

